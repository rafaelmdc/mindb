# Comparison CSV

Required columns:

- `study_doi`
- `study_title`
- `group_a_name`
- `group_b_name`
- `label`

At least one of `study_doi` or `study_title` must be populated in each row.

Optional columns:

- `notes`
