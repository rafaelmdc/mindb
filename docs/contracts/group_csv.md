# Group CSV

Required columns:

- `study_doi`
- `study_title`
- `name`

At least one of `study_doi` or `study_title` must be populated in each row.

Optional columns:

- `condition`
- `sample_size`
- `cohort`
- `site`
- `notes`
